{
  "ConnectionStrings": {
    "SkopiStation": "Server=.\\SQLEXPRESS;Database=SkopiStation;Trusted_Connection=True;TrustServerCertificate=True"
  },
  "Device": {
    // "Fake" emits frames from memory, so the acquisition screen works on a clone with no
    // instrument and no virtual port pair. Set it to "Serial" to read a real port.
    "Mode": "Fake"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  }
}
